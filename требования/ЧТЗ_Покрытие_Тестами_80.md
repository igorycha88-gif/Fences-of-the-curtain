# ЧТЗ: Покрытие тестами UI и сервисов (≥80%)

## 1. Контекст

Текущее покрытие тестами: **57.82% statements, 37.43% branches, 51.4% functions**.
Цель: ≥80% по всем метрикам.

UI-компоненты (75 файлов) — **0% покрытия**.
Часть сервисов и валидаторов не покрыта.

## 2. Цель

Покрыть тестами:
- UI-компоненты (React Testing Library)
- Непокрытые сервисы (admin, calculator, truss)
- Непокрытые валидаторы
- Непокрытые lib-утилиты
- Hooks

## 3. Маршрут: Разработчик

## 4. Декомпозиция

### TASK-INF-01: Установка зависимостей и настройка
- Установить: @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jest-environment-jsdom
- Обновить jest.config.js: testEnvironment для tsx = jsdom, для ts = node
- Добавить setup файл для @testing-library/jest-dom

### TASK-INF-02: Обновить пороги покрытия → 80%

### TASK-BCK-01: Сервисы admin (15 файлов)
- fenceTypeService, gateTypeService, lagTypeService, wicketTypeService
- postTypeService, picketTypeService, meshService
- mountingHardwareService, fenceTypeService
- estimatesService, estimateEditorService, adminCalculatorService
- exportService, auditLogService, usersService, ipLookupService

### TASK-BCK-02: Сервисы calculator (8 файлов)
- fenceCalculator, canopyCalculator, lagCalculator
- gateLookup, wicketLookup, picketLookup
- multiFenceEstimateService, fenceTypeCalculatorService

### TASK-BCK-03: Валидаторы (11 файлов)
- adminCalculator, calculator, trussCalculator
- mesh, adminEstimate, picketType, panel3d
- profnastilType, portfolio, references, wicketType

### TASK-BCK-04: Lib утилиты
- permissions/rbac.ts, lib/utils/fileUpload, priceCalculator, apiResponse, imageUrl
- sectionCalculator, referenceRegistry, analytics, cache-invalidation

### TASK-FRT-01: UI — базовые компоненты
- ui/* (button, input, modal, select, badge, card, label, table, textarea)
- layout/* (Header, Footer, HomeFooter, ContactPhoneBadge)
- seo/* (Breadcrumbs, JsonLd, JsonLdScript, GoogleAnalytics, YandexMetrika)
- reviews/* (RatingStars, ReviewCard, YandexRatingBadge, YandexReviews)
- faq/FaqClient, BackButton

### TASK-FRT-02: UI — калькулятор
- OrderForm, NomenclatureNotFoundModal, CanopyNomenclatureNotFoundModal

### TASK-FRT-03: UI — cookie-consent
- CookieConsentBanner, CookieConsentProvider, CookieConsentSettings

### TASK-FRT-04: UI — admin (приоритет Medium)
- References/*, Orders/*, TrussCalculator/*, Portfolio/*, Works/*

### TASK-FRT-05: Hooks
- useCookieConsent, useScrollReveal

## 5. Критерии приёмки

1. `npm test` — все тесты зелёные
2. `npm run lint` — без ошибок
3. `npx tsc --noEmit` — без ошибок
4. Coverage: statements ≥80%, branches ≥60%, functions ≥80%, lines ≥80%
5. Каждый непокрытый файл имеет соответствующий тест
6. Тесты проверяют: рендеринг, props, события, edge cases, error handling

## 6. Файлы для изменения

- jest.config.js
- package.json (devDependencies)
- __tests__/components/** (новые)
- __tests__/services/** (новые)
- __tests__/lib/** (новые)
- __tests__/hooks/** (новые)

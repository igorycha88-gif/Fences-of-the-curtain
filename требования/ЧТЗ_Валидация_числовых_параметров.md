# ЧТЗ: Безопасная валидация числовых параметров в API

## Версия: 1.1
## Дата: 2026-03-18
## Автор: AI-аналитик
## Приоритет: High
## Статус: Draft

---

## 1. Цели и задачи

### 1.1 Бизнес-цель
Устранить уязвимость inadequate input validation в Admin API путём внедрения централизованной безопасной валидации числовых параметров, предотвращающую инъекции некорректных значений, отрицательные числа и NaN.

### 1.2 Пользовательская ценность
- Защита от DoS атак через передачу некорректных значений пагинации
- Предотвращение ошибок при некорректных параметрах запроса
- Стабильная работа API при любых входных данных
- Единообразная обработка ошибок валидации

### 1.3 Метрики успеха
- **Покрытие**: 100% вызовов parseInt в admin API заменены на safeParseInt
- **Безопасность**: все числовые параметры валидируются (нет NaN, нет отрицательных значений)
- **Отсутствие регрессий**: все существующие API endpoints работают корректно
- **Тестируемость**: unit-тесты для утилиты safeParseInt с покрытием ≥90%

---

## 2. Функциональные требования

### 2.1 User Stories с Acceptance Criteria

#### US-001: Создание утилиты безопасного парсинга
**Как** разработчик  
**Я хочу** иметь централизованную утилиту для безопасного парсинга числовых параметров  
**Чтобы** гарантировать валидацию всех числовых значений из query-параметров

**Acceptance Criteria:**
```
GIVEN утилита safeParseInt создана в src/lib/parse-params.ts
WHEN вызывается safeParseInt(value, fallback)
THEN возвращается число (никогда NaN)
AND возвращается fallback если value === null
AND возвращается fallback если parseInt возвращает NaN
AND возвращается fallback если parsed < 0
AND корректно парсятся положительные числа
```

#### US-002: Защита параметров пагинации
**Как** система безопасности  
**Я хочу**, чтобы параметры page и pageSize всегда были валидными  
**Чтобы** предотвратить ошибки БД-запросов и DoS атаки

**Acceptance Criteria:**
```
GIVEN API endpoint использует safeParseInt для page/pageSize
WHEN передаётся page=-1
THEN используется fallback значение 1
WHEN передаётся pageSize=abc
THEN используется fallback значение 20
WHEN передаётся page=5 (валидное)
THEN используется значение 5
```

#### US-003: Единообразная обработка во всех Admin API
**Как** техлид  
**Я хочу**, чтобы все 13+ admin API endpoints использовали единую утилиту  
**Чтобы** гарантировать одинаковый уровень безопасности везде

**Acceptance Criteria:**
```
GIVEN все файлы src/app/api/admin/*/route.ts
WHEN выполнен поиск parseInt
THEN найдено 0 прямых вызовов parseInt
AND все использования заменены на safeParseInt из @/lib/parse-params
```

---

## 3. Нефункциональные требования

### 3.1 Безопасность
- **Валидация на уровне типов**: TypeScript гарантирует использование утилиты
- **Защита от injection**: все числовые параметры проходят через safeParseInt
- **Fallback стратегия**: при любой ошибке используется безопасное значение по умолчанию
- **Логирование**: (опционально) логировать случаи использования fallback для мониторинга атак

### 3.2 Производительность
- **Минимальные накладные расходы**: утилита должна добавлять <1ms к времени обработки запроса
- **Нет дополнительных зависимостей**: использовать только нативный parseInt

### 3.3 Сопровождаемость
- **Единая точка изменений**: вся логика валидации в одном файле
- **Переиспользование**: утилита может использоваться в любом API endpoint
- **Документация**: JSDoc комментарии с примерами использования

---

## 4. Техническая архитектура

### 4.1 Структура файлов

#### Новый файл: `src/lib/parse-params.ts`
```typescript
/**
 * Безопасный парсинг строкового значения в целое число
 * 
 * @param value - Строка для парсинга или null
 * @param fallback - Значение по умолчанию при ошибке
 * @returns Валидное целое число (никогда NaN или отрицательное)
 * 
 * @example
 * safeParseInt('42', 1)     // → 42
 * safeParseInt(null, 1)     // → 1
 * safeParseInt('abc', 1)    // → 1
 * safeParseInt('-5', 1)     // → 1
 * safeParseInt('0', 1)      // → 0 (ноль валиден)
 */
export function safeParseInt(value: string | null, fallback: number): number {
  const parsed = parseInt(value ?? '', 10)
  return isNaN(parsed) || parsed < 0 ? fallback : parsed
}

/**
 * Безопасный парсинг с ограничением максимального значения
 * Полезно для pageSize чтобы предотвратить слишком большие выборки
 * 
 * @param value - Строка для парсинга или null
 * @param fallback - Значение по умолчанию
 * @param max - Максимальное допустимое значение
 * @returns Валидное целое число в диапазоне [0, max]
 * 
 * @example
 * safeParseIntWithMax('1000', 20, 100)  // → 100 (ограничено max)
 * safeParseIntWithMax('50', 20, 100)    // → 50
 */
export function safeParseIntWithMax(
  value: string | null, 
  fallback: number, 
  max: number
): number {
  const parsed = safeParseInt(value, fallback)
  return Math.min(parsed, max)
}
```

### 4.2 Изменяемые файлы (актуальный список)

| № | Файл | Вызовов parseInt | Строки |
|---|------|------------------|--------|
| 1 | `src/app/api/admin/gate-types/route.ts` | 2 | 20-21 |
| 2 | `src/app/api/admin/lag-types/route.ts` | 2 | 21-22 |
| 3 | `src/app/api/admin/mounting-hardware/route.ts` | 2 | 21-22 |
| 4 | `src/app/api/admin/materials/fence-types/route.ts` | 2 | 19-20 |
| 5 | `src/app/api/admin/picket-types/route.ts` | 2 | 20-21 |
| 6 | `src/app/api/admin/profnastil-types/route.ts` | 2 | 20-21 |
| 7 | `src/app/api/admin/wicket-types/route.ts` | 2 | 19-20 |
| 8 | `src/app/api/admin/orders/[id]/status-history/[index]/route.ts` | 1 | 25 |
| 9 | `src/app/api/admin/estimates/route.ts` | 2 | 13-14 |
| 10 | `src/app/api/admin/works/route.ts` | 2 | 22-23 |
| 11 | `src/app/api/admin/post-types/route.ts` | 2 | 21-22 |
| 12 | `src/app/api/admin/users/route.ts` | 2 | 20-21 |
| 13 | `src/app/api/admin/orders/route.ts` | 2 | 23-24 |

**Итого: 13 файлов, 25 вызовов parseInt**

### 4.3 Паттерн замены

**До:**
```typescript
const page = parseInt(searchParams.get('page') || '1')
const pageSize = parseInt(searchParams.get('pageSize') || '20')
```

**После:**
```typescript
import { safeParseInt } from '@/lib/parse-params'

const page = safeParseInt(searchParams.get('page'), 1)
const pageSize = safeParseInt(searchParams.get('pageSize'), 20)
```

---

## 5. Декомпозиция на задачи

### Backend

#### TASK-BCK-001: Создание утилиты safeParseInt
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.5 часа  
**Зависимости**: нет

**Описание**:
Создать файл `src/lib/parse-params.ts` с функциями safeParseInt и safeParseIntWithMax. Добавить JSDoc документацию.

**Критерии приемки**:
- [ ] Файл `src/lib/parse-params.ts` создан
- [ ] Функция safeParseInt экспортируется
- [ ] Обработаны случаи: null, NaN, отрицательные числа
- [ ] Добавлены JSDoc комментарии с примерами
- [ ] TypeScript компиляция без ошибок

**Технические детали**:
- Файл: `src/lib/parse-params.ts`
- Функция: `export function safeParseInt(value: string | null, fallback: number): number`

---

#### TASK-BCK-002: Рефакторинг gate-types/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Описание**:
Заменить прямые вызовы parseInt на safeParseInt в `src/app/api/admin/gate-types/route.ts`.

**Критерии приемки**:
- [ ] Импорт добавлен: `import { safeParseInt } from '@/lib/parse-params'`
- [ ] Строки 20-21 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-003: Рефакторинг lag-types/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 21-22 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-004: Рефакторинг mounting-hardware/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 21-22 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-005: Рефакторинг materials/fence-types/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 19-20 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-006: Рефакторинг picket-types/route.ts
**Направление**: Backend  
**Приоритет**: Medium  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 20-21 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-007: Рефакторинг profnastil-types/route.ts
**Направление**: Backend  
**Приоритет**: Medium  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 20-21 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-008: Рефакторинг wicket-types/route.ts
**Направление**: Backend  
**Приоритет**: Medium  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 19-20 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-009: Рефакторинг post-types/route.ts
**Направление**: Backend  
**Приоритет**: Medium  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 21-22 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-010: Рефакторинг estimates/route.ts
**Направление**: Backend  
**Приоритет**: Medium  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 13-14 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-011: Рефакторинг works/route.ts
**Направление**: Backend  
**Приоритет**: Medium  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 22-23 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-012: Рефакторинг users/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 20-21 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-013: Рефакторинг orders/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строки 23-24 обновлены
- [ ] Нет прямых вызовов parseInt

---

#### TASK-BCK-014: Рефакторинг orders/[id]/status-history/[index]/route.ts
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-001

**Описание**:
Критичный endpoint с динамическим параметром из URL. Требует особого внимания.

**Критерии приемки**:
- [ ] Импорт добавлен
- [ ] Строка 25: `const historyIndex = safeParseInt(params.index, 0)`
- [ ] Нет прямых вызовов parseInt
- [ ] При невалидном index возвращается fallback 0

---

#### TASK-BCK-015: Финальная проверка admin API
**Направление**: Backend  
**Приоритет**: High  
**Оценка**: 0.25 часа  
**Зависимости**: TASK-BCK-002..TASK-BCK-014

**Описание**:
Выполнить grep по всем файлам admin API и убедиться, что прямых вызовов parseInt не осталось.

**Критерии приемки**:
- [ ] Команда `grep -r "parseInt" src/app/api/admin` не находит результатов
- [ ] Все 25 вызовов заменены на safeParseInt

---

### Testing

#### TASK-TST-001: Unit-тесты для safeParseInt
**Направление**: Testing  
**Приоритет**: High  
**Оценка**: 1 час  
**Зависимости**: TASK-BCK-001

**Описание**:
Создать комплексные unit-тесты для функции safeParseInt. Покрыть все edge cases.

**Критерии приемки**:
- [ ] Файл тестов создан: `src/lib/__tests__/parse-params.test.ts`
- [ ] Покрытие ≥90%
- [ ] Тестовые случаи:
  - [ ] Валидные положительные числа: '1', '42', '100'
  - [ ] Нулевое значение: '0' → 0 (валидно)
  - [ ] Null значение: null → fallback
  - [ ] NaN строки: 'abc', '' → fallback
  - [ ] Отрицательные числа: '-1', '-100' → fallback
  - [ ] Float строки: '3.14' → 3 (parseInt обрезает)
  - [ ] Пробелы: ' 42 ' → 42
  - [ ] Смешанные: '42abc' → 42 (parseInt парсит начало)
  - [ ] Очень большие числа: '999999999999' → parsed value

**Пример теста**:
```typescript
import { safeParseInt } from '../parse-params'

describe('safeParseInt', () => {
  it('should parse valid positive integers', () => {
    expect(safeParseInt('42', 1)).toBe(42)
    expect(safeParseInt('1', 1)).toBe(1)
    expect(safeParseInt('100', 1)).toBe(100)
  })

  it('should return fallback for null', () => {
    expect(safeParseInt(null, 5)).toBe(5)
  })

  it('should return fallback for NaN strings', () => {
    expect(safeParseInt('abc', 10)).toBe(10)
    expect(safeParseInt('', 10)).toBe(10)
  })

  it('should return fallback for negative numbers', () => {
    expect(safeParseInt('-1', 1)).toBe(1)
    expect(safeParseInt('-100', 1)).toBe(1)
  })

  it('should accept zero as valid', () => {
    expect(safeParseInt('0', 1)).toBe(0)
  })
})
```

---

#### TASK-TST-002: Unit-тесты для safeParseIntWithMax
**Направление**: Testing  
**Приоритет**: Medium  
**Оценка**: 0.5 часа  
**Зависимости**: TASK-BCK-001, TASK-TST-001

**Критерии приемки**:
- [ ] Тесты добавлены в `parse-params.test.ts`
- [ ] Тестовые случаи:
  - [ ] Значение в пределах max: safeParseIntWithMax('50', 20, 100) → 50
  - [ ] Значение превышает max: safeParseIntWithMax('150', 20, 100) → 100
  - [ ] Отрицательное + max: safeParseIntWithMax('-5', 20, 100) → 20
  - [ ] Null + max: safeParseIntWithMax(null, 20, 100) → 20

---

#### TASK-TST-003: Integration-тесты Admin API
**Направление**: Testing  
**Приоритет**: Medium  
**Оценка**: 1.5 часа  
**Зависимости**: TASK-BCK-002..TASK-BCK-015

**Описание**:
Проверить что все admin endpoints корректно обрабатывают невалидные query-параметры.

**Критерии приемки**:
- [ ] Тестируются минимум 3 endpoints (users, orders, estimates)
- [ ] Тестовые случаи для каждого endpoint:
  - [ ] GET /api/admin/users?page=-1 → использует fallback (page=1)
  - [ ] GET /api/admin/users?page=abc → использует fallback
  - [ ] GET /api/admin/users?pageSize=999999 → работает без ошибки
  - [ ] GET /api/admin/orders?page=5 → корректно использует page=5
  - [ ] Проверка что возвращается 200 OK (не 500)

---

#### TASK-TST-004: Регрессионное тестирование
**Направление**: Testing  
**Приоритет**: High  
**Оценка**: 1 час  
**Зависимости**: TASK-BCK-002..TASK-BCK-015

**Описание**:
Убедиться что замена parseInt на safeParseInt не сломала существующий функционал пагинации.

**Критерии приемки**:
- [ ] Ручное тестирование 3-5 admin страниц с пагинацией
- [ ] Пагинация работает корректно при валидных параметрах
- [ ] Переход между страницами работает
- [ ] Изменение pageSize работает
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в server logs

**Страницы для тестирования**:
- `/admin/users` - список пользователей
- `/admin/orders` - список заявок
- `/admin/estimates` - список смет
- `/admin/works` - список работ

---

### Documentation

#### TASK-DOC-001: Обновление документации
**Направление**: Documentation  
**Приоритет**: Low  
**Оценка**: 0.5 часа  
**Зависимости**: TASK-BCK-001, TASK-TST-001

**Критерии приемки**:
- [ ] В README.md добавлен раздел "Валидация параметров"
- [ ] Указано использование safeParseInt для всех числовых параметров
- [ ] Приведены примеры использования

---

## 6. Тестирование

### 6.1 Unit-тесты

| Test Case | Input | Expected Output |
|-----------|-------|-----------------|
| Валидное число | `safeParseInt('42', 1)` | `42` |
| Null значение | `safeParseInt(null, 5)` | `5` |
| Пустая строка | `safeParseInt('', 10)` | `10` |
| NaN строка | `safeParseInt('abc', 1)` | `1` |
| Отрицательное | `safeParseInt('-5', 1)` | `1` |
| Ноль | `safeParseInt('0', 1)` | `0` |
| Float | `safeParseInt('3.14', 1)` | `3` |
| С пробелами | `safeParseInt(' 42 ', 1)` | `42` |

### 6.2 Integration-тесты

**Эндпоинты для тестирования**:
- `GET /api/admin/users?page=-1&pageSize=abc`
- `GET /api/admin/orders?page=0&pageSize=-100`
- `GET /api/admin/estimates?page=999999999`

**Ожидаемый результат**: 200 OK с валидными данными, fallback значения применены

---

## 7. Риски и зависимости

### 7.1 Риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Регрессия в существующей пагинации | Medium | High | TASK-TST-004: регрессионное тестирование |
| Пропущенные вызовы parseInt | Low | Medium | TASK-BCK-015: финальная проверка grep |
| Различия в fallback значениях | Low | Low | Чётко задокументировать fallback для каждого параметра |

### 7.2 Rollback план

При обнаружении критических проблем:
1. `git revert` коммита с изменениями
2. Вернуться к прямым вызовам parseInt
3. Проанализировать проблему и исправить утилиту

---

## 8. План внедрения

### 8.1 Последовательность выполнения

```
Шаг 1: TASK-BCK-001 (создание утилиты) - 0.5ч
Шаг 2: TASK-TST-001 (unit-тесты) - 1ч
Шаг 3: TASK-BCK-002..BCK-014 (13 файлов) - 3ч
Шаг 4: TASK-BCK-015 (проверка) - 0.25ч
Шаг 5: TASK-TST-003, TASK-TST-004 (тестирование) - 2.5ч
Шаг 6: TASK-DOC-001 (документация) - 0.5ч
```

**Итого**: ~8 часов (1 рабочий день)

### 8.2 Критерии готовности

- [ ] Утилита safeParseInt создана и протестирована
- [ ] Все 25 вызовов parseInt заменены на safeParseInt
- [ ] Unit-тесты проходят с покрытием ≥90%
- [ ] Регрессионное тестирование не выявило проблем
- [ ] Code review пройден

---

## 9. Согласование

- [ ] Заказчик: _______________
- [ ] Техлид: _______________
- [ ] Security Officer: _______________

---

## 10. Приложения

### 10.1 Полный список файлов с parseInt (актуально на 2026-03-18)

```
src/app/api/admin/gate-types/route.ts:20-21
src/app/api/admin/lag-types/route.ts:21-22
src/app/api/admin/mounting-hardware/route.ts:21-22
src/app/api/admin/materials/fence-types/route.ts:19-20
src/app/api/admin/picket-types/route.ts:20-21
src/app/api/admin/profnastil-types/route.ts:20-21
src/app/api/admin/wicket-types/route.ts:19-20
src/app/api/admin/orders/[id]/status-history/[index]/route.ts:25
src/app/api/admin/estimates/route.ts:13-14
src/app/api/admin/works/route.ts:22-23
src/app/api/admin/post-types/route.ts:21-22
src/app/api/admin/users/route.ts:20-21
src/app/api/admin/orders/route.ts:23-24
```

**Всего**: 13 файлов, 25 вызовов parseInt

### 10.2 Дополнительные файлы вне scope (информация)

```
src/lib/rate-limit.ts:74,148 - 2 вызова (контролируемое использование)
src/services/email/sender.ts:12 - 1 вызов (env переменная)
```

Эти файлы не затрагиваются в рамках данного ЧТЗ, так как:
- `rate-limit.ts` - внутренняя логика rate limiting, значения из Redis
- `sender.ts` - порт SMTP из env, не пользовательский ввод

---

**Документ подготовлен AI-аналитиком**  
**Дата создания**: 2026-03-18  
**Версия**: 1.1
